@{
  Layout = "_Layout";
}

@model Places.Models.City

<h2>Are you sure you want to delete this city?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-outline-danger">
}
<input type="button" class="btn btn-outline-primary" value="Back to details" onclick="location.href='@Url.Action("Details", new { id = Model.CityId })'">